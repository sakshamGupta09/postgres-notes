# Union

- We have multiple SELECT statements that give us some results.
- We want to merge those results into a single result set.
- Result sets to be merged must have same number of columns and data types.
