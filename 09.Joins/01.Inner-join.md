# Inner Join

- Combine data from multiple tables using a related column.
- Return only the matching rows.


```sql
SELECT
  table1.column1,
  table2.column1
FROM table1
JOIN table 2
ON table.key_column = table2.key_column
```

```sql
SELECT
	movies.movie_id,
	movies.movie_name,
	movies.movie_lang,

	directors.first_name,
	directors.last_name
FROM movies
JOIN directors
ON movies.director_id = directors.director_id;
```

## Alias table names
