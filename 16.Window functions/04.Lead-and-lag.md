# Lead and Lag

- To compare a row in an ordered partition with it's previous or next row.
- Like compare the highest salary with the second highest one.
- If the next row or previous does not exists, it returns NULL.

## Syntax

```SQL
SELECT
  *,
  LEAD(column_name),
  LAG(column_name)
FROM table
```
