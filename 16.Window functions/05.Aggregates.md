# Window Aggregate Functions

- When we use group by with aggregation, we get back one row with typically two columns.
- With aggregation, we get back all the rows in a partition plus some additional columns.
- We can use SUM, AVG, MIN, MAX, COUNT.
- Find the employees who are getting paid more than the average salary of each department.

## SUM

- Let's consider we need to find total salaries paid, department wise.
- For every row in a partition, we will get a new column called total salary whoch will use sum aggregation.
- We can compare individual contribution with total value.
- If we do it traditional way, First we will create a table which will hold aggregation of department and total salary, then join it with employees table.

```SQL
SELECT
  employee_id,
  employee_name,
  department,
  SUM(salary) OVER (PARTITION BY department) AS "total_salary"
FROM employees
```
- Order by no longer is necessary.


##  Order by with aggregation function

- If we specify order by with aggregation function, we get a running total (aggregation of all previous rows till now).
- If we use aggregation function without ordering row, we get total else we getting running total.
- If we have duplicate rows for order by column, it will show running total of all duplicate rows as one value.

## Moving window

- When calculating running total, it's taking into consideration all previous rows till current row.
- What if we want a moving window of K rows instead.
