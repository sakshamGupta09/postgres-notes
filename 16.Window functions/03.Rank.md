# Rank

- `Rank`, `DENSE_RANK` and `ROW_NUMBER` give same results when there are no duplicates in the ORDER BY column.
- Two rows with same value will get the same Rank and dense rank.


## Rank vs DENSE_RANK

- Rank creates holes when there are rows with duplicates.
- Dense rank creates compact ranking.
