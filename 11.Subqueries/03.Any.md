# Any

- The subquery returned multiple records.
- Now, for every row in the outer query, we want to check if any value in the result set is satisfying a condition.


## Syntax

```sql
SELECT
  C1,
  C2
FROM table
WHERE
  C1 operator ANY (SUBQUERY)
```
