# to_date(string, format)

- Convert a string to a `DATE` type.
- format is used to describe the format of the input string.
- Output will be a `DATE` type in YYYY-MM-DD format.

```sql
SELECT
	TO_DATE('23/03/2026', 'DD/MM/YYYY')
```

```sql
SELECT
	TO_DATE('March 23, 2026', 'Month DD, YYYY')
```
