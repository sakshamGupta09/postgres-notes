# Transaction Setup

## Begin

- Creates or opens a new transaction block.
- Whtever operations we perform after this are all counted as one transaction.

```sql
BEGIN;
```

## Operations

```sql
UPDATE accounts
SET 
	balance = balance -50
WHERE
	account_id = 1
```
- Since this operation is inside an transaction, this will not be visible to other users unless COMMIT happens.


## Commit

```sql
COMMIT;
```
- I am done with all the operations and now want to sync this with table so that others can see it too.


## ROLLBACK

- If during a transaction block any kind of error occours, syntax, hardware, software, power etc the trasnaction is marked as failed.
- Now it's important to undo all the changes and take system to before transaction state.
