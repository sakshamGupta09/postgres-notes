# Joining Multiple tables

- Resulting table will have columns from three tables.
- Table 1 columns are placed. Then corresponding columns from table 2 are placed. Then columns from table 3.

```sql
SELECT
	*
FROM movies m
JOIN movies_revenues mr
ON m.movie_id = mr.movie_id
JOIN directors d
ON m.director_id = d.director_id
```
