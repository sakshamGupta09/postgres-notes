# All

- We have a result set from subquery containing multiple rows and we have our outer table.
- Select only those rows wherein a all coolumns in result set are satisfying a condition.

## Syntax

```sql
SELECT
	C1,
	C2
FROM Table
WHERE
	column = ALL (SUBQUERY)
```
