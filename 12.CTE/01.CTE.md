# CTE

- Create a temporary table for this query, give it a name, and use it instead of writing everything in one giant unreadable query.
- It can be referenced multiple times in a query unlike a subquery.

## Syntax

```sql
```
