# Concurrently

- While index creation is in porgress, our table is locked. We cannot perform any write operations.
- If it's a production table with several million rows, it's gonna lock our table for a while.


## Concurrently

```sql
CREATE INDEX CONCURRENTLY
```
