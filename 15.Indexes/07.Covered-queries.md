# Covered Queries

- Index is a data structure that holds the column values in an ordered way.
- Lets say we run a query in which we only return the columns which are present in index data structure.
- That way index is sufficient to return the column values and there is no need to go to parent table to get all columns.
- This is a very fast query.
