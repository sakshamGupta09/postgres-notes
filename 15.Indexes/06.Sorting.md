# Sorting

- If we have very large data say several million rows in our table, sorting it is gonna be very very slow.
- Since indexes already stored ordered values, we can use indexes to speed up our sort operations as well.
