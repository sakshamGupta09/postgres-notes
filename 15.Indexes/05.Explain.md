# Explain

- To learn how postgres is executing our query.
- In real time, users are interacting with our application which is quering the database. But how do we know if a query is using the indexes we have setup or not.
