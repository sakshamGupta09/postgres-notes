# Handling Nulls with Group by

- If we group by a column that contains NULL values, the resulting data will have a group with column value as NULL.


## COALESCE

- COALESCE(column value, alternate value if column value is null)

```sql
SELECT
  COALESCE(department, "Not available),
  COUNT(employee_id)
FROM employees
GROUP BY
  department
```
