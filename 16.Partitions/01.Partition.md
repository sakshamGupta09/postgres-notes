# Partitions

- Partitioning is splitting a large table into smaller tables (partitions) based on a rule.
- Examine how a table is queried. What are the most frequently queried columns. Based on these columns we can create partitions so that instead of doing a full table scan, our partitions can provide the results when queried.
- Data should be evenly divided into partitions. Partition sizes should be almost same.
