# Update

- First, we need to filter rows which needs to be updated using a `where` clause.
- For selected rows, I want to update some column values.

``` sql
UPDATE tableName
SET
  C1=V1,
  C2=V2,
  C3=V3
WHERE
  condition
```
