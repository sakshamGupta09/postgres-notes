# Is Null

```sql
SELECT
	*
FROM actors 
WHERE
	add_date = NULL
```

- This query does not work. We cannot use equality checks on `NULL` values.

```sql
SELECT
	*
FROM actors 
WHERE
	add_date IS NULL
```

```sql
SELECT
	*
FROM actors 
WHERE
	add_date IS NOT NULL
```
