# Sub partitions

- Sometimes, a partition can become very bulky. Like if we have country based partitions, India and China will have so many entries in the partition.
- We can create sub partitions of a partition like by region in this example.
