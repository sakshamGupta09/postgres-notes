# Date

- Default format is `YYYY-MM-DD`.
- `CURRENT_DATE` gives us the current date in the system postgres in running on.

## Time

- Time with timezone and Time without timezone.
- Used to store only the time in `HH:MM:SS` format. 
