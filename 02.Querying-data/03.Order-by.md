# Order By

- To sort the data in our table by one or more columns.
- First `From` runs, then `WHERE`, then `SELECT` and then `ORDER BY`.

```sql
SELECT
  *
FROM Table
ORDER BY
  C1 [ASC | DESC],
  C2 [ASC | DESC]
```
