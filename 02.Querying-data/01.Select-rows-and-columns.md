# Selecting data from table

- A table has rows and each rows has columns.
- When we use `SELECT` without `where` clause it gives us all the rows.
- Now, for all those rows, we can control which columns do we want.

