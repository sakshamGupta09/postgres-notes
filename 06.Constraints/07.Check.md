# Check

- Values in a colum must meet a specific criteria.
- For example salary must not be negative. Or age should be between 18 and 55.
- Data types can only help to an extent but if we need to further narrow down our filter, we use check.
- We define a boolean expression and if that returns true data gets inserted.
