# Constraints

- To ensure that only the right data enters our database, we define some rules on table columns.
- Everytime a row is inserted or updated, the values are validated against these rules.
- If rule is voilated, we get an error.
