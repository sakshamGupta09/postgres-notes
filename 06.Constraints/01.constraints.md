# Constraints

- To ensure that only the right data enters our database, we define some rules on table columns or table itself.
- Everytime a row is inserted or updated, the values are validated against these rules.
- If rule is voilated, we get an error.

## Types of constraints

- NOT NULL
