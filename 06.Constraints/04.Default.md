# Default

- While inserting a row, If we do not provide value for a column, it will take a default value instead of storing `NULL`.
- If we have `NOT NULL` constraint, and we do not specify any value, it will throw an error. Instead of failing the operation, we can specify a default value.
