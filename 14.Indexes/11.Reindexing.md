# Reindexing

- If for some reason, our index and original table goes out of sync, we have an option to reindex our indexes.
- We can reindex one specific index, or all the indexes on one table or all the indexes in all the tables in a schema.


```sql
REINDEX INDEX index_name
```

```sql
REINDEX TABLE table_name
```
