# Intersect

- We have different SELECT statements that returns different result sets.
- I want the matching rows in all result sets.
- Number of columns must be the same and their data types should be compatible.

```sql
SELECT
	first_name,
	last_name
FROM actors

INTERSECT

SELECT
	first_name,
	last_name
FROM directors
```
